services:
  - type: web
    name: sistema-vasconia
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn servidor:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: RENDER
        value: true
    plan: free